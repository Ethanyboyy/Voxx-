{
  "$comment": "Durable engineering state. See engineering/README.md. Update at each meaningful checkpoint; a stale entry is worse than none because it gets acted on.",
  "updatedAt": "2026-09-01T00:30:00Z",
  "verifiedAtCommit": "02b6e60",
  "phase": "BRAIN-030 — anatomical asset slot complete, blocked on the NIH GLB bytes",
  "architecture": {
    "stack": "Next.js 16.3 App Router, TypeScript, Prisma 7.9 on SQLite, Vitest, Tailwind, three.js + React Three Fiber",
    "commands": {
      "typecheck": "npm run typecheck",
      "lint": "npm run lint",
      "test": "npm test",
      "build": "npm run build",
      "migrate": "npm run db:migrate"
    },
    "requestFlow": [
      "chat (POST /api/chat) — decideChatAction() runs the deterministic router pass with NO classifier; an empty plan stays conversational, a non-empty one becomes a run. Zero model calls to classify. An orchestrated turn persists its assistant message, starts the run in the BACKGROUND, and returns immediately with run_started; the message is rewritten in place when the run settles.",
      "user request (chat, /agents DriveConsole, or API)",
      "capabilities/router.ts — deterministic pass, then model-assisted fallback; produces a CapabilityPlan",
      "capabilities/orchestrator.ts — sequences the plan, expands capabilities into PlanSteps, wires {{stepN.output}} references, persists traceId + plan",
      "agents/service.ts — createAgentRun persists AgentRun + AgentStep rows; startAgentRun is createAgentRun followed by executeRun. Background mode calls createAgentRun and then executeRun off the response, so there is still exactly ONE execution engine.",
      "agents/executor.ts — THE only execution engine: ordered steps, checkCapability() gate, retries, reference resolution, pause/resume",
      "tools/registry.ts — the closed tool set; each tool declares capability + requiredLevel",
      "capabilities/execute.ts — single provider calls, each wrapped in a CapabilityRun ledger row",
      "capabilities/refine.ts — the bounded improvement loop, owned by ONE step: generate, review, decide, revise, repeat",
      "capabilities/decide.ts — pure decision engine (APPROVE / ITERATE / five stop reasons)",
      "capabilities/revision.ts — turns a review into CHANGE directives plus a PRESERVE list",
      "artifacts/service.ts — append-only ArtifactVersion + ArtifactLink lineage",
      "observability/events.ts recordEvent — the single event writer",
      "capabilities/trace.ts getRunTrace — reassembles everything for the UI"
    ],
    "singleSourcesOfTruth": {
      "execution": "src/lib/agents/executor.ts — no second executor exists or should",
      "authorization": "src/lib/permissions/service.ts checkCapability/enforceCapability — one ladder OBSERVE < ANALYZE < RECOMMEND < ASK < ACT, default grant ANALYZE",
      "events": "src/lib/observability/events.ts recordEvent — the only writer",
      "routing": "src/lib/capabilities/router.ts",
      "tools": "src/lib/tools/registry.ts — closed set; there is deliberately NO arbitrary-command tool",
      "validation": "src/lib/workspace/validate.ts — closed set of four npm scripts, execFile with an argument array, no shell"
    },
    "providerAbstractions": {
      "ai": "src/lib/ai/ (Anthropic + mock; mock has supportsVision false, deliberately)",
      "image": "src/lib/image/ (Gemini/Nano Banana + an Unavailable provider — no mock image provider)",
      "video": "src/lib/video/ (Higgsfield + Unavailable)",
      "generation3d": "src/lib/generation/",
      "research": "src/lib/research/",
      "embeddings": "src/lib/embeddings/",
      "integrations": "src/lib/integrations/"
    },
    "notes": [
      "src/lib/orchestrator/service.ts is NOT the run driver — it is a cross-domain snapshot for prompt context. The run driver is src/lib/capabilities/orchestrator.ts. Same word, different concerns.",
      "AgentRun.traceId is what lets a restarted process reconnect a run to its provider spend. Before it existed the link lived only in memory and in tool arguments.",
      "Sequencing lives in the orchestrator, not the router: the router decides WHICH capabilities are needed, the orchestrator decides what order they can actually run in.",
      "BRAIN_VISUAL_ENGINE.md is the implementation architecture for the next milestone. Section 1 lists what already exists and must be reused; section 9 is the start-here checklist. The Brain Visual Engine is a DEEPENING of the shipping 3D Brain, not a rebuild — src/components/brain/three/ already carries procedural two-hemisphere anatomy, cortical folding, per-state materials, event-driven signal travel, camera rig and quality tiers.",
      "GPU lifecycle was audited at the BRAIN-030 prep checkpoint and found already correct: BrainMesh disposes all five part geometries, ActivityPulse disposes its geometry and material, CameraRig removes both OrbitControls listeners and clears its resume timer, HoloBrain cancels its rAF and disconnects both observers. The real defect was per-frame Vector3 allocation inside useFrame, now hoisted to scratch refs."
    ],
    "decisions": {
      "iteration_loop_shape": "ONE STEP OWNS THE LOOP, not dynamic step insertion. Step references are keyed by step ORDER, so inserting a step renumbers everything after it and silently repoints every existing reference; and resume is currentStep plus per-step status, which insertion invalidates retroactively. The cost — a step is the executor's unit of recovery, so an interrupted loop would restart at attempt 1 — is handled by recovering position from the append-only ArtifactVersions, which carry parameters.attempt.",
      "iteration_trigger": "Two signals, either sufficient: an explicit refinement verb in the request, or a REQUIRED VISUAL_QA step (the router's own signal that the request states a bar to clear). A request with neither gets exactly one generation — inferring 'improve it' would spend money on an assumption.",
      "chat_routing": "The chat branch uses the EXISTING deterministic router pass and nothing else. routeRequest without a classifier returns an empty plan whenever the deterministic pass has no confident opinion, so an ordinary message costs nothing to recognise as ordinary. The orchestrated reply is composed deterministically from the trace rather than by a second model call — cheaper, and a model writing prose about a run it cannot see would describe the run it expects.",
      "chat_async_transport": "The POST returns as soon as the run is DURABLE, not when it finishes. Progress reaches the client over the EXISTING SSE event bus — no second transport, no polling loop, no fabricated tick, and zero extra model calls. The persisted assistant message is the reconnection anchor: it carries the runId in its meta, so a reload finds its way back to work still in flight, and it is rewritten with the real outcome when the run settles. A client disconnect does not cancel the run; only the explicit Stop button does, through the existing cancelRun path.",
      "settle_detection": "The inline panel reports a terminal run on its FIRST non-live read, not on a live→settled transition. A short run can finish before the panel mounts, so the transition may already have happened; waiting for it left the message reading 'Working on it' until a manual reload."
    }
  },
  "verification": {
    "typecheck": {
      "status": "pass",
      "at": "2026-08-31T20:30:00Z"
    },
    "lint": {
      "status": "pass",
      "errors": 0,
      "warnings": 13
    },
    "tests": {
      "status": "pass",
      "files": 68,
      "count": 736,
      "at": "2026-08-31T20:30:00Z"
    },
    "build": {
      "status": "pass",
      "at": "2026-08-31T20:30:00Z"
    },
    "visual": {
      "status": "pass",
      "note": "Real chat UI driven in a browser against a real logged-in session at 1100px and 390px, 0px overflow at both. Measured in a FRESH conversation so no earlier run panel could satisfy the wait: POST returns and the live panel appears at ~270-320ms; the message rewrites itself to the real outcome at ~1.5s with no reload; after a full reload exactly one panel is recovered and the outcome text survives. Three defects were found and fixed in this loop: the step description restated the whole user message (clamped to two lines), the view no longer landed at the newest turn because inline panels grow the list after the scroll effect runs (ResizeObserver re-pin, only when already at the bottom), and a run that settled before the panel mounted left the message permanently on 'Working on it'."
    }
  },
  "providerReality": {
    "$comment": "Reported by each provider's own isConfigured. Never claim connectivity that has not been verified.",
    "image_gemini_nano_banana": "CONFIGURATION REQUIRED — GOOGLE_API_KEY absent",
    "vision_anthropic": "CONFIGURATION REQUIRED — ANTHROPIC_API_KEY absent; Visual QA refuses to run rather than guessing a verdict",
    "video_higgsfield": "CONFIGURATION REQUIRED — HIGGSFIELD_API_KEY absent; egress proxy also prevents verification from this environment",
    "consequence": "End-to-end media workflows are exercised by acceptance tests with injected providers. No live provider call has been verified from this environment."
  },
  "knownLimitations": [
    "MODEL_3D is planned by the router but no tool binds it — the orchestrator emits a described, tool-less step rather than failing on an unknown tool.",
    "Video generation is submit-only: the CapabilityRun stays RUNNING until a later poll closes it. No poller exists yet (ORCH-011).",
    "The improvement loop covers IMAGE generation only.",
    "The inline panel's WAITING_FOR_PERMISSION rendering (awaiting line + Stop button) is covered by tests but was NOT photographed in a browser: in this environment every chat-reachable capability is either already granted to the QA user or has no configured provider, so no chat message could be made to park on permission.",
    "A background run's failure is visible in the rewritten message and the trace, but nothing pushes a notification if the user has closed the tab — they see it on their next visit.",
    "Chat carries no Lab subject context, so a chat-started run cannot attach an artifact to a specific suit — the orchestrator declines rather than picking one arbitrarily.",
    "Resume re-reviews an attempt generated but not yet judged (ORCH-012).",
    "getBrainState() does not read CapabilityRun, the iteration loop or provider calls, so a chat-started run reaches the Brain only as a generic 'executing'. BRAIN-030 phase C fixes this.",
    "Brain state is categorical with no intensity scalar, so concurrency and attempt number are discarded before they reach the visualization.",
    "No DOM test tooling (jsdom/testing-library) is installed, so the mount/unmount subscription-duplication guarantee for useBrainVisualState is enforced by code review, not by a test. useEventStream closes its EventSource in the effect cleanup and there is now exactly one call site on the Brain surface."
  ],
  "sessionHandoff": {
    "$comment": "Where the last session stopped and what the next one does first.",
    "resumeFrom": "02b6e60 (pushed to origin/claude/spider-man-lab-foundation-mvjayy)",
    "blockedOn": "The NIH HRA brain GLB (3DPX-020959 v1.3, CC BY 4.0) is not in the repo. The owner uploaded it to Google Drive as '3d-vh-f-allen-brain.glb', but every Drive MCP call returned 'MCP tool call requires approval' and did not execute — the permission prompt needs approving. 3d.nih.gov itself stays denied by egress policy (403 on CONNECT).",
    "firstActions": [
      "Ask the owner to approve the Google Drive MCP prompt, then search_files for '3d-vh-f-allen-brain.glb'.",
      "VERIFY the retrieved file before touching anything: name, size, and that it parses as a valid GLB.",
      "Copy to public/models/brain/nih-hra-brain-female/brain.hero.glb.",
      "Run tools/3d-pipeline/inspect_glb.ts and REPORT the measured numbers before integrating.",
      "Then: fill asset.json, register in public/models/index.json, map real node names into components[].meshNames.",
      "Verify whole-brain / X-ray / cutaway / rotated / 390px, and only then judge the anatomical acceptance test."
    ],
    "doNot": [
      "No further procedural anatomy changes — that approach is rejected and the reason is recorded in BRAIN-030.proceduralVerdict.",
      "No GitHub mirror of the NIH model, no substitute brain mesh, no routing around the egress denial.",
      "Do not claim the anatomical Brain works until the NIH mesh is visibly rendering."
    ],
    "deployment": "NEVER DEPLOYED. fly.toml, Dockerfile and DEPLOYMENT.md are ready but this environment's network policy blocks fly.io outright (api.fly.io returns 000), flyctl is not installed and no FLY_API_TOKEN exists. Deployment must be run by the owner per DEPLOYMENT.md. There is no live URL."
  }
}